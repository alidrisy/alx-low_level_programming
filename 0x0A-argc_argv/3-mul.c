#include "main.h"
#include <stdio.h>

/**
* main - multiplies two numbers.
* @argc: numbers of arguments
* @argv: pointer
* Return: error or 0
*/

int main(int argc, char *argv[])
{
int i;
for (i = 0; i < argc; i++)
{
x = 0;
x *= atoi(argv[i]);
}
