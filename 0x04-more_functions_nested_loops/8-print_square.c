#include "main.h"

/**
* print_square - print line.
*@size: the number of \ character.
*/
void print_square(int size);
{
int x, z;
if (size <= 0)
{
_putchar('\n');
}
else
{
for (x = 0; x < size; x++)
{
if (x >= 0)
{
for (z = 0; z < x=; z++)
_putchar('#');
}

_putchar('\n');
}

}
}
