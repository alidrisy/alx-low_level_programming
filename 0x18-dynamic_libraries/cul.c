/**
* add - add tow integer
* @a: int
* @b: int
* Return: a + b
*/

int add(int a, int b)
{
return (a + b);
}

/**
* sub - subtract tow integer
* @a: int
* @b: int
* Return: a - b
*/
int sub(int a, int b)
{
return (a - b);
}

/**
* mul - multiply tow integer
* @a: int
* @b: int
* Return: a * b
*/
int mul(int a, int b)
{
return (a * b);
}

/**
* div - div tow integer
* @a: int
* @b: int
* Return: a / b
*/
int div(int a, int b)
{
return (a / b);
}

/**
* mod - module tow integer
* @a: int
* @b: int
* Return: a % b
*/
int mod(int a, int b)
{
return (a % b);
}

