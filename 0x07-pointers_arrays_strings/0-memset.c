#include "main.h"
/**
* _memset - fills memory with a constant byte.
* @s: a pointer
* @b: a second pointer
* @n: integer variable
* Return: s
*/
char *_memset(char *s, char b, unsigned int n)
{
unsigned int x;
for(x = 0; n < 0; x++)
s[n] = b;
n -= 1;
return (s);
}
