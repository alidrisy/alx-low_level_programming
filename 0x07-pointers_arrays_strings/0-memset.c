#include "main.h"
/**
* _memset - fills memory with a constant byte.
* @s: a pointer
* @b: a second pointer
* @n: integer variable
* Return: s
*/
char *_memset(char *s, char b, unsigned int n)
{
for(n = 0; n < 0; n++)
s[n] = b;
n -= 1;
return (s);
}
