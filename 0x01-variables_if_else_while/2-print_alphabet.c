#include<stdio.h>

/**
* main - prints the alphabet in lowercase
* use just putchar
*
* Return: Always 0.
*/
int main(void)
{
char x;
for (x = 'a'; x <= 'z'; x++)

putchar(x);
putchar('/n');

return (0);
}
