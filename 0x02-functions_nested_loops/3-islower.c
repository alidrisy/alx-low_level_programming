#include "main.h"

/**
* _islower - checks for lowercase character.
* @c: a variable
* Return: if is lower 1 else 0.
*/

int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else

return (0);

}
